[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Bruno Martins notes from the field"
authors = [{name = "Bruno Martins", email = "bruno@bmartins.pt"}]
dynamic = ["version", "description"]
